    #include<bits/stdc++.h>
    using namespace std;
    int main(void)
    {
    string s="xyzbc";
    sort(s.begin(),s.end());

    size_t w;
    w = s.find('b');

    if(w==string::npos)
    cout<<"Found"<<endl;

    else
    cout<<"Not Found"<<endl;
    return 0;
   }
